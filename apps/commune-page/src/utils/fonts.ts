import { Cairo, Oxanium } from "next/font/google";

export const cairo = Cairo({
  subsets: ["latin"],
  display: "swap",
});

export const oxanium = Oxanium({
  subsets: ["latin"],
  display: "swap",
  weight: "300",
});
