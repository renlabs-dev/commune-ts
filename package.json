{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "deploy-commune-page": "yarn run build && (cd apps/commune-page/ && npx @cloudflare/next-on-pages@1)",
    "deploy-commune-governance": "yarn run build && (cd apps/commune-governance/ && npx @cloudflare/next-on-pages@1)"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.6.1",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
